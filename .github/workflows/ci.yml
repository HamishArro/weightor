name: CI workflow

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on:
      group: ubuntu-runners

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
        - run: npm i